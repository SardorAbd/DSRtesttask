describe("registration form page", () => {
    it('verify the page is loaded and registration form is rendering properly', () => {
        cy.visit('https://vladimirwork.github.io/web-ui-playground/')
    })
})